<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= pokemon.name %></title>
    <style>
        body {
            text-align: center;
            background-color: blueviolet;
            font-family: cursive;
        }
        .headings {

            color: antiquewhite;
        }

        form {
            display: flex;
            flex-wrap: wrap;
            flex-direction: column;
            margin: 10px 0;
        }
        input {
            margin: 10px auto;
            padding: 10px;
            width: 80%;
            border: 8px solid black;
            border-radius: 10px;
        }
        #submitButton {
            color: red;
            font-size: 20px;
        }
        button {
            font-size: 20px;
            font-family: cursive;
        }
        span {
            background-color: orange;
        }
        a {
            text-decoration: none;
            color: black;
        }
        a:hover {
            text-decoration: underline;
        }

        #editInfoHeading {
            display: flex;
            justify-content: center;
            gap: 20px;
        }
    </style>
</head>
<body>

    <h1 class="headings">Edit A Current Pokemon</h1>
    <h2 id="editInfoHeading" class="headings">Edit <a href="/pokemon/<%= pokemon._id %>"><span><%= pokemon.name %>'s</span></a>Info</h2>
    <form action="/pokemon/<%= pokemon._id %>?_method=put" method="post">
        <input type="text" name="name" placeholder="Name of Pokemon" value="<%= pokemon.name %>" />
        <input type="text" name="type" placeholder="Type of Pokemon" value="<%= pokemon.type %>" />
        <h2 class="headings" style="text-decoration: underline;">Edit Stats</h2>
        <input type="number" name="stats.hp" placeholder="Amount of hp" value="<%= pokemon.stats.hp %>" />
        <input type="number" name="stats.attack" placeholder="Amount of Attack" value="<%= pokemon.stats.attack %>" />
        <input type="number" name="stats.defense" placeholder="Amount of Defense" value="<%= pokemon.stats.defense %>" />
        <input type="number" name="stats.spattack" placeholder="Amount of spAttack" value="<%= pokemon.stats.spattack %>" />
        <input type="number" name="stats.spdefense" placeholder="Amount of spDefense" value="<%= pokemon.stats.spdefense %>" />
        <input type="number" name="stats.speed" placeholder="Speed of Pokemon" value="<%= pokemon.stats.speed %>" />
        <input type="submit" id="submitButton" value="Update Pokemon" />
    </form>
    <a href="/pokemon"><button>Go Back To HQ</button></a>
</body>
</html>